import styles from './login.module.scss';

const Register = () => {
    return (
        <div className={`${styles.register_wrapper}`}>
            <p className='color-primary-30'>This is register</p>
        </div>
    )
}

export default Register;