import styles from './login.module.scss';

const Login = () => {
    return (
        <div className={`${styles.login_wrapper}`}>
            <p className='color-primary-10'>This is Login</p>
        </div>
    )
}

export default Login;